pkgname=paddle2onnx-bin
pkgver=1.0.5
pkgrel=1
epoch=
pkgdesc="ONNX Model Exporter for PaddlePaddle"
arch=('x86_64')
url="https://github.com/chubin/cheat.sh"
license=('Apache')
groups=()
makedepends=()
checkdepends=()
provides=('paddle2onnx')
conflicts=('paddle2onnx')
replaces=()
backup=()
options=()
install=
changelog=
source=("https://github.com/PaddlePaddle/Paddle2ONNX/releases/download/v$pkgver/${pkgname%-bin}-linux-x64-$pkgver.tgz")
md5sums=('SKIP')

package() {
    cd "${pkgname%-bin}-linux-x64-$pkgver"
    cp -rv . "$pkgdir/"
}
